#include<stdio.h>
int main()
{
inta[25],m,i;
float mean=0,sum=0;
printf("enter the terms");
scanf("%d",&m);
for(i=1;i<=m;i++)
{
scanf("%d",a[i]));
}
sum=sum+a[i];
avg=sum/m;
printf("mean entered number is %f",mean);
}
