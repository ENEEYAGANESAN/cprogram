#include<stdio.h>
#include<string.h>
int main()
{
    char a[20];
    int count =0,special character;
    printf("enter the string");
    get(str);
    for(count=0;str[count]!=null;count++)
    if(str[count]>='!' && str[count]<='+")
    {
    printf("the special character is:%d",special character);
    }
    else
    {
    printf("there is no special characters");
    } 
    return 0;
    }
    
